If you haven’t done so, make the script executable in the project folder:

chmod +x setup_plus_run.sh

Run the Script
You can now execute the script, and it will install Node.js, npm, Vue CLI, and your project dependencies, and finally run your Vue.js project:

then, run the following:

./setup_plus_run.sh
